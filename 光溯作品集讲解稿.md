---
title: 光溯作品集讲解稿
aliases:
  - 光溯面试讲解稿
  - LightTrace Portfolio Talk Track
tags:
  - 作品集
  - 面试准备
  - AI产品经理
  - 光溯
status: ready
created: 2026-08-05
updated: 2026-08-05
source_pages:
  - "[[index.html|产品概览]]"
  - "[[architecture.html|架构设计]]"
  - "[[case-study.html|诊断案例]]"
  - "[[evidence.html|评测与治理]]"
---

# 光溯作品集讲解稿

> [!abstract] 使用方式
> - **完整讲解**：按“产品概览 → 架构设计 → 诊断案例 → 评测与治理”的顺序，约 15～20 分钟。
> - **单页讲解**：每一页都配有独立开场和独立收束，可按面试官兴趣单独展开，约 3～6 分钟。
> - **临场原则**：先讲产品判断，再讲页面信息；先讲为什么，再讲怎么做；指标只讲口径明确、自己能解释的数据。
> - 文中的“承接句”用于同一页面的模块切换，“转场句”用于切换到下一张网页。

## 一句话定位

光溯是一套面向复杂金融业务链路的智能诊断产品：它把用户的模糊报错转成一个可执行的诊断任务，沿业务链路组织日志、知识和专项工具，最终输出有证据、可回放、可由专家复核的候选原因与待核动作。

## 开场总结概述

### 30 秒版本

> 我想介绍的项目叫“光溯”，它解决的是复杂金融交易里业务现象和技术根因脱节的问题。过去一次报错往往要在业务、应用、网络、系统多个团队之间反复转交，信息散落、判断依赖专家经验。我的核心思路不是再做一个通用 Chatbot，而是把排障过程产品化：先确认业务场景，再匹配链路、采集证据，按条件调用专项诊断能力，最后输出带证据引用和人工复核边界的诊断结果。整个作品集分成四页，分别回答“产品是什么”“系统为什么这样设计”“一次诊断怎么跑”“质量如何被验证”。

### 90 秒版本

> 这个项目叫“光溯 LightTrace”，定位是复杂金融业务链路智能诊断。它来自一个非常典型的企业问题：用户只看到一句模糊报错，但真实原因可能分布在业务交易、应用调用、网络状态、数据库或中间件等多个层级。传统排障通常靠截图和聊天记录在团队之间流转，每个角色只掌握一段上下文，最后高度依赖少数专家把信息拼起来。
>
> 我在这个项目里的核心判断是，AI 的价值不只是把回答写得更像专家，而是把一次跨团队排障收束成一条可执行、可复核的诊断任务。所以产品先把自然语言或截图转成结构化场景和关键槽位，用户确认后再关联业务链路，沿链路检索日志与知识；网络、系统等专项能力只有在证据满足条件时才触发。输出也不是一个过度确定的“根因答案”，而是候选原因、关键证据、待核动作和人工复核入口。
>
> 整套作品集我用四个问题来组织：第一，为什么需要这个产品以及它创造什么价值；第二，如何通过确定性主流程和条件 Skills 控制 Agent 的行为边界；第三，一条真实风格的诊断任务是怎样被回放的；第四，如何按不同路径评测，并通过 Trace、门禁和 Bad Case 回流持续治理质量。

**总述后的转场句：**

> 我先从产品概览开始，讲清楚它解决的核心矛盾，以及为什么我没有把它定义成一个普通的智能问答工具。

---

# 第一页：产品概览

页面：[[index.html|产品概览]]

## 单独讲这一页时的开场

> 这一页主要回答三个问题：光溯解决什么问题、产品如何形成差异化，以及它最终带来了什么业务价值。最核心的一句话是：把跨团队排障，从信息转交变成一条证据逐步收敛的诊断任务。

## 模块 1：核心问题与产品定位

> 先看产品要解决的问题。复杂金融交易的链路很长，用户看到的是“登录失败”或者“网络开小差”，但背后可能经过几十个应用节点。业务、应用、网络和系统团队分别掌握不同片段，报错截图在群里转交，排查动作散落在聊天记录里，最终结论非常依赖个人经验。
>
> 所以我没有把光溯定义成一个替代专家的问答机器人，而是定义成一个诊断任务的组织者。它从业务现象出发，自动补齐场景和链路，再跨应用、网络、系统等层级取证，把候选原因、证据摘要和下一步待核动作放在同一个上下文里。产品真正减少的是无效转交和重复取证，而不是简单减少一次搜索。

**承接句：**

> 问题定义清楚以后，下一步就是看系统具体替用户承担了哪些复杂性。

## 模块 2：四项核心能力

> 第一项能力是理解复杂业务场景。用户可以输入文本或截图，系统先识别业务现象、环境和关键定位信息，不会因为一句模糊报错就直接启动全链路排查。
>
> 第二项是组织跨层取证。系统把业务拓扑、日志检索、知识库和专业诊断工具编排成工作流，并根据已有证据决定下一步，而不是让 Agent 自由调用所有工具。
>
> 第三项是输出可复核结果。结论必须同时带候选原因、关键证据和待核动作；证据不足时保留不确定性并交给人工确认。
>
> 第四项是让诊断能力持续变好。每一次运行都会留下可回放的 Trace，用于失败归因、回归评测和知识更新。这样一次诊断不只是消费知识，也会成为下一轮治理的输入。

**承接句：**

> 这四项能力落到用户体验上，不应该变成一个复杂的工具面板，而应该是一条非常清楚的诊断路径。

## 模块 3：诊断主路径

> 用户侧看到的主路径可以概括为六步：输入报错或截图、理解业务场景、匹配应用链路、检索日志与知识、按证据调用专项能力、输出待核动作。
>
> 这里最重要的不是步骤数量，而是流程的确定性。场景识别、链路匹配、证据采集和报告生成是主干；网络、数据库或系统诊断是条件分支。触发了什么、为什么触发，没有触发什么、为什么跳过，都会被记录下来。这样流程才可解释、可回放，也能被评测。

**承接句：**

> 这条路径背后，其实对应三个我在产品设计中坚持的行为边界。

## 模块 4：三个产品判断

> 第一个判断是“先确认场景，再锁定链路”。业务场景决定应用链路，链路决定日志范围，日志再决定异常位置。模型不能跳过中间环节直接编一个听起来合理的根因。
>
> 第二个判断是“两阶段检索”。第一次检索面向用户原始描述，目标是及时回应并补齐信息；第二次检索面向系统已经发现的异常节点和日志证据，目标是收敛处理知识。两次检索的输入、时机和失败后果都不同。
>
> 第三个判断是“结论必须指回证据”。日志片段、接口关系或知识条目必须能支撑结论；证据不足时，只能给候选原因和待核动作，不能为了答案完整而越过证据边界。

**承接句：**

> 有了这些边界，最后才有资格讨论业务结果，因为这些数字必须建立在可解释的诊断口径上。

## 模块 5：业务价值与指标

> 在核心长链路场景里，光溯把原本小时级、多人参与的排查过程收敛到分钟级辅助诊断闭环。这里的“分钟级”指从用户提问开始，完成场景确认、链路定位和日志分析，最终形成一份标准的辅助诊断结果，并不是宣称完全自动解决故障。
>
> 在协同范围上，典型问题原本最多需要四个技术部门联合排查，产品将范围收敛到一个主责部门定向复核，对应 75% 的排查范围收敛。
>
> 在诊断质量上，四类核心长链路场景的候选根因命中率超过 85%。这个口径是“Agent 给出的候选范围包含人工确认的真实故障原因”就算命中，最终根因仍由专业人员确认。
>
> 除核心链路诊断外，标准化能力还覆盖领域问答、异常容器监控和版本部署辅助。页面展示了 4.8/5 的问答满意度、6.2 万多条有效异常告警，以及 84% 的版本部署报错诊断样本准确率。这些数据说明同一套诊断思路可以被嵌入不同技术场景，但我会把它们和核心长链路指标分开讲，避免混成一个笼统的“AI 准确率”。

## 本页收束

> 所以这一页想传达的不是“我做了一个会查日志的 Agent”，而是我把一个跨团队、强依赖专家经验的排障过程，抽象成了有入口边界、有证据链、有人工复核机制的产品流程。

## 转到第二页：架构设计

> 产品定位讲清楚后，一个自然的问题是：怎样让 Agent 既能跨层取证，又不因为自由度过高而失控？下一页我重点讲这套产品判断是如何落实到架构和运行机制里的。

## 如果只讲这一页，结束语

> 如果只用一句话总结，这个产品的差异化不在于接了多少模型和工具，而在于把“什么时候可以继续判断、结论必须由什么证据支撑”做成了产品规则。如果您感兴趣，我可以继续展开架构，或者直接用一个登录失败案例回放整条路径。

---

# 第二页：架构设计

页面：[[architecture.html|架构设计]]

## 单独讲这一页时的开场

> 这一页我不想做技术栈罗列，而是想解释几个关键产品决策如何进入系统架构：怎样统一跨层上下文、怎样限制 Agent 的自由度、怎样让每次判断有证据并可回放。

## 模块 1：四域能力与共享底座

> 整体架构分成业务、应用、网络和系统四个诊断域。每个域有自己的入口、知识资产和专业能力，但它们共享同一套诊断上下文、数据证据和质量框架。
>
> 业务域负责识别交易场景和匹配链路；应用域负责定位节点、分析日志和识别异常；网络域只在有连通性或策略证据时触发；系统域覆盖数据库、中间件、资源和状态线索。中间的诊断引擎负责把这些域串起来：保存槽位值、字段来源和确认状态，执行确定性主流程和条件 Skills，再生成证据约束报告。
>
> 为什么必须共享底座？因为一个业务报错往往先表现为应用异常，例如 RPC 超时，但它并不天然等于网络问题。只有共享上下文，系统才能跨层路由，同时保留每个专业域的责任边界。

**承接句：**

> 四域架构解决的是“能力如何组织”，再往下看一次任务具体怎样在这些能力之间流动。

## 模块 2：条件诊断主流程

> 用户输入自然语言、截图或附件后，系统先做意图识别和槽位抽取，把场景、环境、现象、定位主键和时间窗口变成结构化状态，并记录每个字段来自哪里、是否已经确认。
>
> 接下来有两个并行过程：一边做领域知识检索，先给用户一个及时回应并提示缺失字段；另一边匹配候选业务场景，等待用户确认。这里故意不直接进入深诊断，因为模糊输入不足以确定业务边界。
>
> 如果用户的问题只是领域问答，就留在应用、系统或网络域内继续交互；只有场景和关键槽位确认后，系统才形成统一诊断上下文，进入业务链路、日志、定向知识检索、条件 Skills、报告门禁和人工复核这条深度路径。
>
> 这里还要区分两次检索：首轮检索基于用户知道的信息，深度检索基于系统找到的异常证据。两者不能合并，否则很容易用泛化知识替代真实诊断。

> [!note] 实现边界，面试时可主动说明
> 原课题方案由模型按字段抽取并通过对话补齐；公开技术验证为了可复现，当前先用规则或正则抽取并以 Pydantic 校验。面向生产的方案是 LLM Structured Output 与确定性规则混合。这样既不把 Demo 包装成完整生产实现，也能说明演进方向。

**承接句：**

> 这条流程看起来有很多节点，但真正决定系统行为的，仍然是三个很明确的设计边界。

## 模块 3：三个行为边界

> 第一，先确认场景，再形成任务。系统不会在不知道“是什么业务”的情况下开始排查。
>
> 第二，两次检索承担不同职责。用户描述只能帮助建立候选方向，日志证据才能帮助收敛处理知识。
>
> 第三，主流程固定，子诊断按条件触发。场景确认、链路匹配、证据采集和报告生成保持确定性；网络、数据库和系统诊断根据证据选择，跳过也要留痕。
>
> 这三点共同回答了一个问题：光溯与通用 Chatbot 的区别，不是能调用多少工具，而是什么条件下允许模型继续判断。

**承接句：**

> 如果说前半部分是一次诊断的产品流程，那么下面这部分就是如何把它做成可维护、可协作的运行系统。

## 模块 4：场景资产、Workflow、Skills 与 MCP 的协同

> 这张协同图可以从上到下看。最上方是责任分工：业务和测试 Owner 维护场景、用户现象和业务规则；技术和应用 Owner 维护拓扑、数据来源和节点检索；AI 产品和平台 Owner 负责 Workflow、Skills、MCP 策略、评测和发布。
>
> 中间从左到右是一条运行主干。第一列是场景资产工作台，把专家经验沉淀成 Scene Package、Topology、节点查询配方和关联计划。第二列是 Workflow 或 Harness，它把这些配置组装成确定性主流程，并通过 Skill Registry 决定每一步为什么调用。第三列是 MCP Tool Runtime，负责连接日志平台、CMDB、知识库、监控和配置中心，同时处理 Schema、鉴权、超时和降级。第四列是证据化结果，包括 AI 决策卡、人工复核、Diagnostic Trace 和反馈回流。
>
> 这里我刻意拆开了 Skill 和 Runtime 的职责：Skill 决定为什么调用、需要什么上下文；Runtime 负责权限和实际执行。这样业务判断不会和企业系统凭据、接口细节耦合在一起。
>
> 上下文也不是不断增长的聊天记录，而是拆成 Run Context、Working State、Correlation Ledger、Evidence 和 Checkpoint。每个 Skill 只拿到完成当前任务所需的字段和安全引用，既控制噪声，也控制敏感信息暴露。

**承接句：**

> 有了运行机制之后，技术选型才有意义，因为每个组件都必须服务于可控、可追溯和可回放。

## 模块 5：技术取舍

> 编排层选择 LangGraph 或 StateGraph，是因为它比纯 Chain 或完全自由的 Agent 更适合表达固定主流程、条件分支和回放状态。
>
> 推理层使用长上下文模型承接日志和证据引用，视觉模型主要做截图文字与界面上下文提取，不把视觉理解当作诊断核心。
>
> 检索和工程层采用适合中文专业术语的嵌入、向量库与 FastAPI，重点不是组件本身，而是让知识检索、工作流状态和工程接口能够稳定协同。
>
> 总体取舍可以概括为三句话：编排优先于对话，证据优先于表达流畅，诊断质量优先于单一回答分数。

**承接句：**

> 最后再看扩展性：这套架构不是只服务一个工作台，而是可以把同样的证据逻辑嵌入不同技术系统。

## 模块 6：规模、公开边界与扩展能力

> 原课题沉淀了 5 万多条标准化错误码及解决方案、6 万多条应用信息与配置、80 万多条服务调用链路，以及 170 多份技术手册、测试和安装资料。这里要强调，价值不在于把资料堆进知识库，而是让它们进入场景、链路和诊断流程。
>
> 同一底座还能延伸到异常容器监控、智能网络诊断、性能测试分析和版本部署辅助，形成嵌入式 Copilot 能力。
>
> 同时我会主动说明公开边界：这些规模数据来自原课题沉淀；公开 Demo 使用的是由公开资料改写的知识卡和匿名 fixture，两者不是同一层生产数据。这样既保护真实业务信息，也保证作品集陈述可核验。

## 本页收束

> 这一页的核心不是我用了哪些框架，而是我如何把业务场景、专家知识、企业工具和质量治理组织成一套可维护的诊断系统，并通过确定性主流程限制 Agent 的行为边界。

## 转到第三页：诊断案例

> 架构讲的是跨场景可复用的规则。接下来我用一个“手机银行登录失败”的案例，把这些抽象设计放回一次具体诊断里，看每一步到底改变了什么判断。

## 如果只讲这一页，结束语

> 总结来说，这套架构的关键词是“统一上下文、条件调用、证据化结果和可回放状态”。如果您更关注实际体验，我可以接着回放一个完整案例；如果更关注上线质量，我也可以直接讲评测和治理。

---

# 第三页：诊断案例

页面：[[case-study.html|诊断案例]]

## 单独讲这一页时的开场

> 这一页用一个匿名化的手机银行登录失败案例，展示光溯怎样把一句“四套环境都提示网络开小差”转成一条有场景、有链路、有证据边界的诊断任务。

## 模块 1：从模糊报错到可确认槽位

> 用户最初提供的信息非常模糊：四套环境手机银行登录失败，页面提示“网络开小差”，并附了一个定位关键字。这里最容易犯的错误，是被文案里的“网络”两个字带偏，直接进入网络排查。
>
> 光溯先做意图识别和槽位抽取。它识别出这是业务场景类问题，提取环境、现象和定位主键，同时发现时间窗口仍然缺失。随后一边通过 RAG-1 给出初步证据摘要和补充信息提示，一边返回候选业务场景。只有用户确认“手机银行登录”和关键槽位后，系统才创建统一诊断上下文。

**承接句：**

> 场景确认解决了“我们到底在查什么”，下一步才是沿着正确的业务链路回答“异常发生在哪里”。

## 模块 2：沿 26 节点链路取证

> 系统匹配到一条包含 26 个节点的匿名业务拓扑，关联应用节点、接口和日志路径，然后沿链路检索日志。最终在账户核查节点发现 RpcTimeout，耗时 5000 毫秒。
>
> 这时系统启动 RAG-2。它不是再搜用户原来的“网络开小差”，而是基于异常节点和 RpcTimeout 错误模式，命中 Dubbo 客户端超时排查知识卡。知识卡的作用是补充排查方向和待核动作，不是替代运行证据直接宣布根因。

**承接句：**

> 找到 RPC 超时以后，真正考验系统的不是还能调用多少工具，而是能否克制住不必要的推断。

## 模块 3：服务检查、网络触发与分支留痕

> RPC 超时本身还不足以证明网络故障，所以系统先根据拓扑确定目标服务，通过 MCP adapter 检查实例、注册、接口响应和线程池，四项服务状态没有发现异常。RPC 日志与服务侧检查结果共同满足跨层路由条件后，系统才触发网络诊断。
>
> 匿名网络探测发现 TCP 间歇超时、18% 丢包和 P95 高延迟。系统与数据库分支因为没有对应错误证据保持跳过，并在 Trace 中保存原因。这里既展示了证据如何触发跨层诊断，也展示了没有依据的分支不会被顺手调用。

**承接句：**

> 因此，最终报告不是把一个异常包装成确定答案，而是把当前已经成立的证据和仍需验证的部分明确分开。

## 模块 4：跨层证据报告与界面呈现

> 最终报告引用三层证据：RPC 日志说明异常发生在什么调用，服务状态 MCP 说明当前没有发现实例、注册、接口响应和线程池异常，网络探测则给出间歇超时、丢包和高延迟。基于这三层证据，系统把匿名样本中的问题收敛为网络链路质量异常。
>
> 这里的报告边界仍然很重要：结论不是由“RPC 超时”这一条证据直接推出来的，而是由服务检查和网络探测逐层补齐；结论也只限定在版本化匿名 fixture 内，不外推为真实生产环境结论。
>
> 页面上采用用户视图与专家证据分层呈现：左侧保持连续对话，右侧展示任务进度、链路、决策卡、证据和诊断结果。这样普通用户看到的是一条清楚的处理路径，专家则可以下钻到每个判断背后的证据。

**承接句：**

> 把整次回放压缩来看，真正改变判断的只有六个节点。

## 模块 5：六个可复核节点与案例结论

> 第一，识别意图并形成槽位状态；第二，确认手机银行登录场景并建立统一上下文；第三，沿 26 个节点定位 RPC 超时并命中排查知识；第四，执行服务状态 MCP 检查；第五，触发网络诊断，同时让系统和数据库分支保持跳过；第六，合并 RPC 日志、服务状态和网络探测证据，生成范围受限的诊断报告。
>
> 这个案例最想证明的是：RPC 超时本身不是网络根因，但它可以成为跨层诊断的起点。只有服务侧检查未发现异常、网络探测又提供了间歇超时、丢包和高延迟证据后，系统才能把匿名样本收敛到网络链路质量异常。

## 本页收束

> 所以这次案例证明的是跨层证据收敛：系统从模糊现象定位到 RPC 异常，再通过服务检查和网络探测补齐证据；该触发的网络分支被触发，没有证据的系统和数据库分支保持跳过。

## 转到第四页：评测与治理

> 一条案例讲通，只能证明这个设计在一个样本上合理。要让它成为可上线、可迭代的产品，还需要回答：怎样把这些判断变成自动检查的质量门禁，怎样知道一次升级没有破坏原来的路径？这就是最后一页的评测与治理。

## 如果只讲这一页，结束语

> 这个案例可以用一句话总结：系统没有因为“网络开小差”或 RPC 超时就直接下结论，而是先检查服务状态，再触发网络诊断，最终用 RPC 日志、服务状态和网络探测三层证据，把匿名样本收敛到网络链路质量异常。

---

# 第四页：评测与治理

页面：[[evidence.html|评测与治理]]

## 单独讲这一页时的开场

> 这一页回答的是 AI 产品上线后最关键的问题：质量到底怎么定义。我的判断是，光溯不能把所有请求混在一起算一个准确率，因为业务深诊断、领域问答和日志检索的目标、风险和失败方式完全不同。

## 模块 1：质量判断框架

> 我先把质量拆成四个问题。第一，路径选对了吗——应该留在领域问答，还是升级为深度诊断；第二，证据够不够——回答能否指回知识、日志、工具结果和业务链路；第三，结论有没有越界——证据不足时是否保留候选、拒答或转人工；第四，版本能否稳定回归——知识、模型、工具或流程变化后，固定样本是否还能按原路径运行。

**承接句：**

> 有了统一的问题框架，还需要先把真实请求拆成不同的执行路径，否则指标依然会失真。

## 模块 2：三类入口、四种执行方式

> 第一类是业务场景深诊断，它的链路最长、证据源最多，也是端到端趋势跟踪的主对象。核心指标包括场景解析命中、候选根因命中、关键证据门禁、工具路径正确性和诊断耗时。
>
> 第二类是领域 Agent 问答。应用、系统或网络问题进入受控领域 Agent，通过 RAG、按需工具和追问完成回答、拒答或升级。它更关注领域路由、检索忠实度、工具和参数正确性、多轮上下文以及正确拒答。
>
> 第三类是日志检索。技术用户已经给出结构化条件时，可以跳过前置自然语言理解。这里又分成按业务场景读取整条链路，和按应用或节点直接查询两种执行方式，所以页面说“三类入口、四种实际执行方式”。它关注条件有效性、链路或节点覆盖、日志召回、证据选择和任务生成。
>
> 这三类路径分别评测、分别归因，不混算一个“全链路准确率”。

**承接句：**

> 路径需要拆开，但底层的证据、工具和版本不能各自为政，所以我又设计了一套横向质量底座。

## 模块 3：四层统一质量底座

> 第一层是知识与回答，检查检索命中、上下文精度、回答忠实度、引用有效性和知识来源。
>
> 第二层是工具与数据，检查参数、执行状态、结果回传，以及链路、节点和日志是否真实可用。
>
> 第三层是路径与结论，检查跨层路由、证据合并、条件分支、拒答升级和报告门禁。
>
> 第四层是发布与回归，检查匿名边界、版本冻结、Runner 回放和历史样本退化。
>
> 这四层是横向框架，不代表每条请求都必须走完四层。业务深诊断覆盖最完整；领域问答和日志检索则按各自风险选择评测深度。

**承接句：**

> 统一框架解决“看哪些维度”，下一步还要把关键产品判断写成机器可以执行的契约。

## 模块 4：代表案例的 10 项门禁

> 我把代表案例里的产品边界编码成 10 类检查，包括路径顺序、必备证据、工具调用、跨层路由、报告引用、禁止断言、检索可用性、结论强度和接口回归。
>
> 最关键的一项是“RPC 超时不能直接断言为网络根因”。它不再是一条写在 PRD 里的文案要求，而是一个可以自动失败、被固定成回归样本的质量门禁。在当前代表案例中，服务工具和网络工具都有可审计记录，网络分支按证据触发，数据库保持跳过，最终报告必须引用 RPC 日志、服务状态和网络探测三类证据。

**承接句：**

> 契约定义的是预期，Trace 则负责告诉我们实际运行时到底发生了什么。

## 模块 5：分路径 Trace

> 业务深诊断 Trace 覆盖请求理解、缺失字段、候选场景、用户确认、业务链路、工具参数、条件分支、证据引用、报告门禁和人工复核。
>
> 领域 Agent Trace 更关注领域路由、检索引用、工具决策以及追问、拒答或升级。
>
> 日志检索 Trace 则记录场景或节点模式、链路读取、日志查询、异常聚类、时间线、证据选择和任务生成。
>
> 所以 Trace 不是简单保存模型最后说了什么，而是保存“为什么走这条路、哪些证据进入了结论、哪些分支被跳过”。

**承接句：**

> 有了 Trace，单次运行才能和预先定义的期望进行对照，形成真正可用的评测记录。

## 模块 6：评测记录与 Bad Case 修复闭环

> 一次评测记录分成三层。第一层是 Expected，也就是预先定义的基准契约，包括期望路径、必备证据、禁止输出和通过条件；第二层是 Observed，保存模型输出、工具调用、证据链和版本信息；第三层是 Judged，对照两者给出通过状态、差异项和失败断点。
>
> 不是每次运行都直接变成 Bad Case。所有运行都有评测记录，只有失败、低置信或被人工标记异常的记录，经过复核后才进入修复闭环。
>
> 修复闭环是五步：收集失败诊断、按路径归因、执行修复、冻结回归样本、进入版本门禁。失败标签会区分领域路由、错误拒答、场景解析、链路缺失、日志召回、工具失败、知识缺失和结论越界。这样团队才能知道应该改路由、RAG、拓扑、工具，还是报告门禁，而不是笼统地说“模型不准”。

**承接句：**

> 很多失败最后会落到知识问题，但知识治理也不能简单等于把更多文档塞进向量库。

## 模块 7：知识治理

> 三类路径共享同一套可追溯的知识与证据资产，包括业务场景、应用节点、接口、错误码、日志入口和处理经验。每个对象都要有来源、版本、责任域、适用环境、敏感级别和失效状态，并通过关系层连接到具体场景和工具。
>
> 原课题中，我推动 7 个正式参与部门按知识领域分工共建知识资产。这里有三个治理原则：知识命中不等于根因确认，检索失败不等于知识缺失，动态更新也不等于自动入库。变更必须确认来源、范围、责任和受影响路径，再触发相应的样本回归。

**承接句：**

> 最后，我会把个人职责和团队协同边界说清楚，避免把整个课题的集体成果都包装成个人完成。

## 模块 8：我的工作与协同边界

> 我在项目中主导课题方向与产品定义，搭建智能诊断工作流和整体架构，并推动跨团队的知识标准与评测体系建设。我的核心产出是路径抽象、诊断边界、评测口径、质量门禁和治理机制。
>
> 同时，真实课题里的业务链路梳理、日志平台对接、知识资产整理和工程落地是团队共同完成的。我会明确区分个人主导的产品与架构判断，以及依赖业务、技术和平台团队协同建设的部分。

## 本页收束

> 这套评测体系的核心，不是给模型一个总分，而是让每个失败都能回答三个问题：错在哪条路径、断在哪个证据点、应该由哪一层能力负责修复。只有这样，Agent 产品才具备可发布、可回归和可持续治理的基础。

## 完整作品集结束语

> 回到整个项目，光溯做的事情可以概括为三步：把模糊报错结构化，把跨层排障证据化，把模型运行可治理化。产品概览定义了问题和价值，架构页把诊断边界落实成确定性主流程，案例页证明系统能在证据不足时保持克制，评测页则把这些判断编码成可回放、可失败、可修复的质量机制。
>
> 对我来说，这个项目最能体现的不是单一功能设计，而是我如何在复杂业务、AI 能力不确定性和跨团队协作之间建立一套可落地的产品结构。如果您对其中某一块更感兴趣，我可以继续展开场景抽象、Agent 工作流、评测指标，或者具体讲我在跨团队推进中的做法。

## 如果只讲这一页，结束语

> 一句话总结：我没有把评测做成一个笼统的答案准确率，而是按路径定义期望、保存运行证据、定位失败断点，再把异常样本送回知识、工具和流程迭代。这也是我认为企业级 Agent 从 Demo 走向产品的关键一步。

---

# 面试现场速查

## 四页各自只记一句

1. **产品概览**：把跨团队排障收束成一条证据逐步收敛的诊断任务。
2. **架构设计**：用统一上下文、确定性主流程和条件 Skills 控制 Agent 的行为边界。
3. **诊断案例**：从 RPC 超时出发，用服务状态和网络探测把匿名样本收敛到网络链路质量异常。
4. **评测治理**：按路径定义指标，用契约和 Trace 把失败定位到可修复的能力层。

## 四个高频追问的短答

### 为什么不用通用 Chatbot？

> 因为诊断的关键不是语言生成，而是场景、链路、证据和责任边界。通用 Chatbot 容易跳过中间证据直接给结论，而光溯把“何时可以继续判断”做成了确定性流程。

### 为什么要两次 RAG？

> 第一次基于用户原始描述，负责及时回应和补齐信息；第二次基于日志异常，负责收敛处置知识。两次检索面对的证据成熟度和失败风险不同。

### 为什么 RPC 超时不能算网络根因？

> RPC 超时只是调用未在规定时间返回，可能由服务实例、线程池、连接池、网络或变更等多种因素引起。当前案例是在服务状态检查未见异常、网络探测又发现间歇超时、18% 丢包和 P95 高延迟后，才把匿名样本收敛到网络链路质量异常。

### 你个人具体做了什么？

> 我主导课题方向和产品定义，负责诊断路径抽象、整体工作流与架构、评测口径、质量门禁和治理机制；链路梳理、平台对接、知识整理与工程落地由跨团队共同建设。

> [!tip] 临场表达提醒
> 不要逐字念页面，也不要连续报组件名。每个模块优先讲“问题 → 判断 → 机制 → 结果”，说完一句收束，再询问面试官是否希望继续下钻。
